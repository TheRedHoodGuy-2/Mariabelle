{
  "name": "tensura-project",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:whatsapp": "yarn workspace whatsapp-bot dev",
    "dev:discord": "yarn workspace discord-bot dev",
    "dev:dashboard": "yarn workspace dashboard dev",
    "build:all": "yarn workspaces run build",
    "typecheck:all": "yarn workspaces run typecheck"
  },
  "devDependencies": {
    "@types/qrcode-terminal": "^0.12.2",
    "typescript": "^5.4.0"
  },
  "packageManager": "yarn@1.22.22",
  "dependencies": {
    "dotenv": "^17.3.1",
    "qrcode-terminal": "^0.12.0"
  }
}
